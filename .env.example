# Compose reads this file automatically for local demo runs.
# This repo stays intentionally dev/demo-oriented and does not ship a full
# production settings stack.

DJANGO_DEBUG=1
DJANGO_SECRET_KEY=
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0,web

OPENAI_API_KEY=
OPENAI_REFINE_MODEL=
